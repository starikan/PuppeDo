# Список общих папок для git worktree задач (.worktrees/<имя>/).
# create-worktree.sh создаёт в task-worktree относительные symlink на эти пути в корне основного репозитория.
# Строки с # и пустые строки игнорируются. Один путь на строку, относительно корня репозитория.
.agents-sessions
.tasks
