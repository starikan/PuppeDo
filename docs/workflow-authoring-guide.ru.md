# PuppeDo: Полный гайд по проектированию workflow (для ИИ‑агента)

> **Актуальность:** январь 2026
> **Источники:** код `src/Test.ts`, `src/model.ts`, `src/Defaults.ts`, плагины `src/Plugins/*`, примеры `tests/*`

Этот документ описывает принципы, правила и нюансы построения workflow (тестов/атомов) в PuppeDo. Цель — дать ИИ‑агенту исчерпывающие инструкции для генерации корректных новых workflow.

---

## 1) Базовые понятия

### 1.1 Agent (тест/атом)
В PuppeDo все «блоки» — это **агенты**. Они описываются в YAML/PPD/JSON и обязательно имеют поле `name`. Агенты бывают:

| Тип | `type` | Назначение |
|-----|--------|------------|
| **test** | `test` или без type | Сценарий верхнего уровня |
| **atom** | `atom` | Переиспользуемый шаг (действие) |

Технически структура одинакова — различие семантическое. В workflow шаг вызывается по имени:

```yaml
- atomName:
    параметр: значение
```

Например: `clickWithNestedWait`, `blank`, `goTo`, `typeInput`.

### 1.2 Lifecycle
Список этапов жизненного цикла задаётся в конфиге (см. [puppedo.config.js](puppedo.config.js)):

`PPD_LIFE_CYCLE_FUNCTIONS: ['beforeRun', 'beforeTest', 'run', 'runTest', 'afterRun', 'afterTest']`

Каждый этап — это массив шагов. Этапы выполняются в указанном порядке. Даже если в YAML используется только `run` или только `runTest`, фактически движок готов к любому из этапов.

**Ключевая мысль:** workflow — это дерево агентов; в каждом узле можно использовать любые этапы из `PPD_LIFE_CYCLE_FUNCTIONS`.

### 1.3 Runner (окружение)
Runner — конфигурация браузера/среды. Определяется в файлах типа `type: runner`. В тесте можно переключаться между runners через atom `runnerSwitch`/`runnerClose`.

**Поддерживаемые движки (engine):**
- `playwright` — предпочтительный, поддерживает chromium, firefox, webkit
- `puppeteer` — legacy, только chromium

### 1.4 Data/Selectors
Data и Selectors — отдельные файлы `type: data` / `type: selectors`, которые подключаются к runner и/или тесту через `dataExt`/`selectorsExt`.

### 1.5 Плагины
PuppeDo поддерживает плагины для расширения функциональности. Каждый плагин регистрируется автоматически из папки `src/Plugins/*`. Ключевые плагины:

| Плагин | Назначение |
|--------|------------|
| `argsRedefine` | Переопределение аргументов |
| `continueOnError` | Продолжение при ошибках |
| `debug` | Отладка (пауза, breakpoint) |
| `descriptionError` | Кастомные сообщения ошибок |
| `engineSupports` | Ограничение по движку |
| `frame` | Работа с iframe |
| `logOptions` | Настройки логирования |
| `options` | Наследование опций |
| `selectors` | Расширенная работа с селекторами |
| `skipSublingIfResult` | Пропуск следующих шагов |

## 2) Где берутся файлы и как они мерджатся

### 2.1 Где искать
Корни поиска задаются аргументами:
- `PPD_ROOT` (обычно `tests`)
- `PPD_ROOT_ADDITIONAL` (в проекте подключены `src/Plugins`)

Обход всех файлов с расширениями: `.yaml`, `.yml`, `.ppd`, `.json`.
Исключения задаются `PPD_ROOT_IGNORE` и `PPD_FILES_IGNORE`.

### 2.2 Несколько документов в одном файле
YAML поддерживает несколько документов через `---`. Это активно используется (например data.yaml содержит 2 документа).

### 2.3 Уникальность имён
**Имя (`name`) обязательно.** Дубли недопустимы (строго проверяется по типу). При обнаружении дублей — ошибка.

## 3) Форматы файлов

### 3.1 Data / Selectors
Минимальный формат:

- `type: data` или `type: selectors`
- `name: <string>`
- `data: <object>`

Пример:

```yaml
---
type: data
name: data
data:
  baseUrl: http://localhost:3000/
```

### 3.2 Runner
Runner содержит конфигурацию браузера и подключаемых data/selectors:

- `name` — имя runner-а
- `type: runner`
- `data`, `selectors` — локальные значения
- `dataExt`, `selectorsExt`, `runnersExt` — подключаемые внешние файлы
- `browser` — объект конфигурации:
  - `engine`: `playwright` | `puppeteer`
  - `browserName`: `chromium` | `firefox` | `webkit`
  - `headless`: `boolean`
  - `args`: массив CLI-аргументов браузера
  - `windowSize`: `{ width, height }`
  - `runtime`: дополнительные параметры запуска
- `log` — уровни логирования, screenshot/fullpage по умолчанию

Пример:
```yaml
name: mainRunner
type: runner
browser:
  engine: playwright
  browserName: chromium
  headless: false
  args:
    - "--start-maximized"
  windowSize:
    width: 1920
    height: 1080
dataExt:
  - auth
selectorsExt:
  - selectors
```

В проекте есть два runner‑а: [tests/runners/mainRunner.yaml](tests/runners/mainRunner.yaml) и [tests/runners/mainRunnerPuppeteer.yaml](tests/runners/mainRunnerPuppeteer.yaml).

### 3.3 Agent (test/atom)
Основной интерфейс описан в типе `TestTypeYaml` (см. [src/model.ts](src/model.ts)).

**Основные поля:**

| Поле | Тип | Описание |
|------|-----|----------|
| `name` | `string` | **Обязательно.** Уникальное имя агента |
| `type` | `string` | `test`, `atom`, `runner`, `data`, `selectors` |
| `description` | `string` | Человекочитаемое описание |
| `bindDescription` | `string` | JS-выражение для динамического описания |
| `descriptionExtend` | `string[]` | Расширенные описания для документации |
| `todo` | `string` | Пометка о незавершённой работе |
| `note` | `string` | Комментарий/заметка |

**Данные и селекторы:**

| Поле | Тип | Описание |
|------|-----|----------|
| `data` | `object` | Локальные данные |
| `selectors` | `object` | Локальные селекторы |
| `dataExt` | `string[]` | Подключаемые data-файлы |
| `selectorsExt` | `string[]` | Подключаемые selectors-файлы |
| `bindData` | `object` | JS-выражения для вычисления данных |
| `bindSelectors` | `object` | JS-выражения для вычисления селекторов |
| `bindResults` | `object` | Результаты / вычисления результатов |

**Валидация:**

| Поле | Тип | Описание |
|------|-----|----------|
| `needData` | `string[]` | Обязательные ключи данных (суффикс `?` = опционально) |
| `needSelectors` | `string[]` | Обязательные селекторы |
| `needSelector` | `string[]` | Синоним `needSelectors` (единственное число) |
| `needEnvParams` | `string[]` | Обязательные переменные окружения |
| `allowResults` | `string[]` | Разрешённые ключи результатов |
| `allowOptions` | `string[]` | Разрешённые опции для потомков |

**Управление выполнением:**

| Поле | Тип | Описание |
|------|-----|----------|
| `repeat` | `number` / `string` | Количество повторений (или JS-выражение) |
| `while` | `string` | JS-условие продолжения цикла |
| `if` | `string` | JS-условие выполнения |
| `errorIf` | `string` | JS-условие для ошибки |
| `errorIfResult` | `string` | JS-проверка результатов |
| `breakParentIfResult` | `string` | JS-условие прерывания родителя |
| `tags` | `string[]` | Теги для фильтрации запуска |
| `disable` | `boolean` | Отключить выполнение |

**Плагин-специфичные поля:**

| Поле | Тип | Описание |
|------|-----|----------|
| `continueOnError` | `boolean` | Продолжить при ошибке |
| `skipSublingIfResult` | `string` | JS-условие пропуска следующих шагов |
| `frame` | `string` | Селектор iframe для контекста |
| `engineSupports` | `string[]` | Разрешённые движки (`playwright`, `puppeteer`) |
| `descriptionError` | `string` | Кастомное сообщение при ошибке |

**Прочее:**

| Поле | Тип | Описание |
|------|-----|----------|
| `inlineJS` | `string` | Inline JavaScript код |
| `logOptions` | `object` | Настройки логирования (см. раздел 9) |
| `options` | `object` | Произвольные опции |

А также **любые** этапы жизненного цикла (`beforeRun`, `run`, `runTest`, …) как массивы шагов.

## 4) Алиасы (ключевая часть синтаксиса)

Алиасы задаются в [puppedo.config.js](puppedo.config.js) через `PPD_ALIASES`. Это критично для чтения/генерации workflow. Все алиасы объединяются.

### 4.1 Список алиасов из проекта

**data**:
- `d`, `📋`

**bindData** (вычисление значений):
- `bD`, `bd`, `📌📋`, `dataBind`, `db`, `dB`, `dataFunction`, `dF`, `df`, `🔑📋`, `functionData`, `fd`, `fD`

**selectors**:
- `selector`, `s`, `💠`

**bindSelectors** (вычисление селекторов):
- `bindSelector`, `bS`, `bs`, `📌💠`, `selectorBind`, `selectorsBind`, `sb`, `sB`, `selectorsFunction`, `selectorFunction`, `sF`, `sf`, `🔑💠`, `functionSelector`, `functionSelectors`, `fs`, `fS`

**bindResults** (результаты + вычисления):
- `bindResult`, `bR`, `br`, `result`, `results`, `r`, `↩️`, `R`, `rb`, `rB`, `resultBind`, `resultsBind`, `rF`, `rf`, `🔑↩️`, `functionResult`, `fr`, `fR`, `resultFunction`, `values`, `value`, `v`, `var`, `vars`, `const`, `c`, `let`, `set`

**options**:
- `option`, `opt`, `o`, `⚙️`

**Важно:** все алиас‑ключи мерджатся (см. `resolveAliases`), поэтому в одном шаге можно сочетать, например, `data` и `d`, и они будут объединены.

## 5) Порядок формирования данных в агенте

Логика описана в [src/Test.ts](src/Test.ts). Итоговые наборы данных:

### 5.1 Data
Порядок (слева направо, позже может переопределить):
1. `PPD_DATA` (глобальные аргументы)
2. `runner.getRunnerData().data`
3. `dataExt` (внешние data‑файлы)
4. `dataParent` (данные родителя)
5. `resultsFromPrevSubling` (результаты предыдущих siblings)
6. `data` (локальные data агента)
7. `bindData` (вычисленные выражения, могут переопределять)

### 5.2 Selectors
Порядок аналогичен:
1. `PPD_SELECTORS`
2. `runner.getRunnerData().selectors`
3. `selectorsExt`
4. `resultsFromPrevSubling`
5. `selectors`
6. `bindSelectors`

### 5.3 Проверка пересечений
Если ключ одновременно в data и selectors **и значения различаются**, будет ошибка.

### 5.4 needData / needSelectors
Списки обязательных параметров. Если ключ не найден — ошибка. Суффикс `?` делает параметр необязательным.

После проверки `need*` ключи дополнительно **добавляются в data/selectors с null**, если отсутствуют (для унификации структуры).

### 5.5 repeat / $loop
`repeat` вычисляется как JS‑выражение (строка) на основе `allData`.
Затем в `allData`, `dataLocal`, `selectorsLocal` добавляются:
- `repeat` — текущее значение счётчика (убывает от N до 1)
- `$loop` — текущая итерация (возрастает от 1)

Логика: `$loop` берётся из `inputs.dataParent?.repeat` либо равен `repeat`.

**Важно:** при `repeat > 1` агент выполняется повторно с:
- `repeat = repeat - 1`
- новый `stepId` с суффиксом `-repeat-<n>`
- результаты предыдущей итерации внедряются в data/selectors следующей

### 5.6 Специальные переменные в контексте

В контексте выполнения доступны:

| Переменная | Описание |
|------------|----------|
| `repeat` | Текущее значение счётчика повторений |
| `$loop` | Номер итерации (альтернативное представление) |
| `stepId` | Уникальный идентификатор шага |
| `PPD_*` | Глобальные параметры конфигурации |

## 6) Выражения и вычисления

### 6.1 Где можно использовать выражения
Исполняемые выражения (через `runScriptInContext`) применяются в:
- `bindData`, `bindSelectors`, `bindResults`
- `if`, `errorIf`, `errorIfResult`
- `while`
- `repeat`
- `bindDescription`

Контекст выражения — `allData` (data + selectors + результаты). В `bindResults` доступны также результаты предыдущих lifecycle‑функций.

### 6.2 Поведение при ошибке
- В `bindData`/`bindSelectors` ошибки вычисления **не фейлят** шаг: значение остаётся исходной строкой.
- В `if`/`errorIf`/`errorIfResult` ошибки приводят к **ошибке выполнения**.

### 6.3 Примеры

```yaml
bindData:
  text: "'Hello ' + userName"

if: "count > 0"
errorIf: "status !== 'ok'"
errorIfResult: "resultCode !== 200"

repeat: "items.length"
while: "shouldContinue === true"
```

## 7) Условия и управление выполнением

### 7.1 disable
Если `disable: true`, агент пропускается. После этого флаг сбрасывается, чтобы не ломать вложенные циклы.

```yaml
- myAtom:
    disable: true  # Шаг будет пропущен
```

### 7.2 if / errorIf
- `if`: если выражение ложно — агент **пропускается** (без ошибки)
- `errorIf`: если выражение истинно — **ошибка**

```yaml
# Выполнить только если есть данные
- processData:
    if: "items && items.length > 0"

# Ошибка если статус неверный
- checkStatus:
    errorIf: "status !== 'active'"
```

### 7.3 errorIfResult
Проверка **после** вычисления результатов агента. Если выражение истинно — **ошибка**.

```yaml
- getText:
    s: { selector: header }
    r: { text: text }
    errorIfResult: "text === ''"  # Ошибка если текст пустой
```

### 7.4 breakParentIfResult
Если выражение истинно — агент **прерывает** выполнение родительского уровня (цикла) и возвращает `localResults` наверх. Используется для досрочного выхода из циклов.

```yaml
name: searchInLoop
runTest:
  - blank:
      repeat: 10
      run:
        - checkCondition:
            r: { found: result }
            breakParentIfResult: "found === true"  # Выйти из цикла если найдено
```

### 7.5 skipSublingIfResult (плагин)
Позволяет на основе выражения пропускать **следующие** шаги того же уровня (siblings). Полезно для условного завершения последовательности.

```yaml
runTest:
  - step1:
      r: { shouldSkip: flag }
      skipSublingIfResult: "shouldSkip === true"
  - step2:  # Будет пропущен если shouldSkip === true
  - step3:  # Будет пропущен если shouldSkip === true
```

Внутри цикла:
```yaml
- blank:
    repeat: 5
    run:
      - checkItem:
          skipSublingIfResult: "$loop === 3"  # На 3-й итерации пропустить остальные шаги
      - processItem:  # Пропускается при $loop === 3
```

### 7.6 tags
Если задан `PPD_TAGS_TO_RUN`, агент выполнится только при пересечении `tags` с этим списком.

```yaml
name: adminTest
tags:
  - admin
  - smoke
runTest:
  - adminAction:
```

При запуске с `PPD_TAGS_TO_RUN=['smoke']` тест выполнится. При `PPD_TAGS_TO_RUN=['regression']` — пропустится.

### 7.7 continueOnError
Работает только если глобально `PPD_CONTINUE_ON_ERROR_ENABLED=true`. Тогда при `continueOnError: true` ошибка **не прерывает** поток, а логируется и выполнение продолжается.

```yaml
- riskyStep:
    continueOnError: true  # Продолжить даже при ошибке
```

### 7.8 engineSupports (плагин)
Ограничивает выполнение атома определёнными движками браузера.

```yaml
name: playwrightOnlyAtom
engineSupports:
  - playwright
run:
  - someAction:  # Выполнится только в playwright
```

Если текущий движок не в списке — атом пропускается.

## 8) Результаты

### 8.1 Возврат результатов
- Каждый атом/тест может вернуть объект результатов.
- `allowResults` ограничивает допустимые ключи.
- `bindResults` может вычислять или переименовывать результаты.

### 8.2 Повтор (repeat)
Если `repeat > 1`, агент перезапускается с:
- `repeat = repeat - 1`
- `stepId` суффиксом `-repeat-<n>`
- результаты прошлого шага внедряются в `data` и `selectors` следующего запуска.

### 8.3 while
Если `while` возвращает true, `repeat` увеличивается на 1, что позволяет динамически расширить цикл.

## 9) Логирование и скриншоты

### 9.1 logOptions
`logOptions` управляет логированием шага. Полная структура (из [src/model.ts](src/model.ts)):

```typescript
type LogOptionsType = {
  logThis?: boolean;        // Логировать этот шаг (default: true)
  logChildren?: boolean;    // Логировать дочерние шаги (default: true)
  logShowFlag?: boolean;    // Показывать флаг в логе
  screenshot?: boolean;     // Делать скриншот элемента
  fullpage?: boolean;       // Делать скриншот всей страницы
  screenshotName?: string;  // Имя файла скриншота элемента
  fullpageName?: string;    // Имя файла скриншота страницы
  level?: string;           // Уровень логирования
  textColor?: string;       // Цвет текста в логе
  backgroundColor?: string; // Цвет фона в логе
}
```

Пример использования:
```yaml
- importantStep:
    logOptions:
      textColor: green
      backgroundColor: black
      screenshot: true
      screenshotName: "important_element"
```

### 9.2 Скриншоты
Если `logOptions.screenshot=true`, движок ищет элементы по `needSelectors` и делает снимки. Поэтому:
- Всегда указывайте `needSelectors` и `bindSelectors`, если нужен element screenshot.

Пример из [tests/tests/screenshots.yaml](tests/tests/screenshots.yaml):
```yaml
- blank:
    description: Screenshot with custom name
    needSelectors:
      - input
    bS:
      input: input
    logOptions:
      screenshot: true
      screenshotName: "myScreenshot"
```

### 9.3 description и bindDescription
- `description` — статическое описание шага
- `bindDescription` — JS-выражение для динамического описания

```yaml
- processItem:
    description: "Обработка элемента"
    bindDescription: "'Обработка элемента #' + $loop + ' из ' + total"
```

Если `description` пустой при выводе, лог подсвечивается красным как предупреждение.

### 9.4 descriptionExtend
Массив расширенных описаний, выводится в режиме документации (`PPD_LOG_DOCUMENTATION_MODE`). Используется для генерации документации из тестов.

```yaml
- loginStep:
    description: Авторизация
    descriptionExtend:
      - "Шаг 1: Открыть страницу логина"
      - "Шаг 2: Ввести учётные данные"
      - "Шаг 3: Нажать кнопку входа"
```

### 9.5 descriptionError (плагин)
Кастомное сообщение ошибки, вычисляемое в контексте данных.

```yaml
- checkValue:
    descriptionError: "'Ожидалось значение ' + expected + ', получено ' + actual"
    errorIf: "actual !== expected"
```

### 9.6 todo и note
Поля для документирования незавершённой работы и комментариев:

```yaml
- incompleteStep:
    todo: "Добавить обработку крайних случаев"
    note: "Временное решение, требует рефакторинга"
```

## 10) Options

Плагин options наследует настройки от родителя к дочерним агентам (см. [src/Plugins/options/options.ts](src/Plugins/options/options.ts)).

### 10.1 Использование options
```yaml
- parentStep:
    options:
      timeout: 5000
      retries: 3
    run:
      - childStep:  # Наследует timeout и retries
```

### 10.2 allowOptions
Ограничивает список опций, которые могут использовать дочерние агенты:

```yaml
name: restrictedAtom
allowOptions:
  - timeout
  - visible
run:
  - action:
      options:
        timeout: 1000   # ОК
        visible: true   # ОК
        # retries: 3    # Будет проигнорирован
```

## 10.3 frame (плагин)
Позволяет выполнять действия внутри iframe:

```yaml
- actionInFrame:
    frame: "iframe.content-frame"  # CSS селектор iframe
    run:
      - clickSelector:
          s: { selector: button }
```

Вложенные шаги выполняются в контексте указанного фрейма.

## 11) inlineJS

В некоторых атомах (например `blank`) можно использовать `inlineJS` для выполнения пользовательского JS.

### 11.1 Контекст выполнения
В контексте `inlineJS` доступны:
- `this.data` — текущие данные
- `this.selectors` — текущие селекторы
- `this.options` — текущие опции
- `this.page` — объект страницы (Playwright/Puppeteer)
- `this.runner` — текущий runner
- `this.log` — объект логирования
- `this.getEngine()` — получить движок

### 11.2 Пример использования
Из [tests/tests/main.yaml](tests/tests/main.yaml):

```yaml
- blank:
    description: Custom keyboard input
    needData:
      - customText
    needSelector:
      - input
    bS:
      input: input
    bD:
      customText: "'Hello World'"
    inlineJS: |
      const el = await this.page.$(this.selectors.input);
      await el.type(this.data.customText);
      return { typed: true };
```

### 11.3 Валидация параметров
Используйте `needData` и `needSelector` (или `needSelectors`) для валидации входных данных перед выполнением `inlineJS`.

### 11.4 Возврат результатов
Возвращайте объект результатов, если нужно прокинуть данные дальше:

```yaml
inlineJS: |
  const text = await this.page.textContent(this.selectors.element);
  return { extractedText: text, success: true };
```

**Рекомендации:**
- Не изменяйте `this` напрямую
- Всегда обрабатывайте возможные ошибки
- Используйте `await` для асинхронных операций
- Возвращайте понятные результаты

## 12) Рекомендованный стиль построения workflow

### 12.1 Именование
- Ясные и стабильные `name` (уникальные в рамках типа).
- Описательные `description`.

### 12.2 Структура
- Используйте `beforeRun`/`beforeTest` для подготовки окружения (runnerSwitch, goTo).
- `run`/`runTest` — основной поток шагов.
- `afterRun`/`afterTest` — cleanup (runnerClose).

### 12.3 Data/Selectors
- Максимально выносите константы в `data`/`selectors` файлы.
- В шагах используйте `bindData`/`bindSelectors` для вычислений.

### 12.4 Предсказуемые циклы
- Используйте `repeat` только для детерминированных циклов.
- Для динамики — `while` с чётким условием.

### 12.5 Ошибки
- Для проверок используйте `errorIf` / `errorIfResult`.
- Для условного пропуска — `if`.

## 13) Шаблоны (минимальные)

### 13.1 Минимальный тест
```yaml
name: myTest
runTest:
  - goTo:
      bindData: { url: baseUrl }
```

### 13.2 Тест с подготовкой и очисткой
```yaml
name: loginFlow
beforeTest:
  - runnerSwitch:
      data: { runnerName: mainRunner }
  - goTo:
      bindData: { url: baseUrl }

runTest:
  - typeInput:
      bS: { selector: loginInput }
      d: { text: userLogin }
  - clickSelector:
      bS: { selector: loginButton }

afterTest:
  - runnerClose:
      data: { runnerName: mainRunner }
```

### 13.3 Использование результатов
```yaml
name: getHeaderText
runTest:
  - getText:
      s: { selector: header }
      r: { headerText: text }
  - blank:
      errorIfResult: "headerText !== 'Welcome'"
```

## 14) Частые ошибки и как их избегать

1. **Дубли имён** — всегда проверять уникальность `name` в пределах типа.
2. **Смешение data и selectors** — одинаковые ключи с разными значениями приведут к ошибке.
3. **Нет нужных параметров** — корректно задавать `needData`/`needSelectors`.
4. **Забыли bindSelectors** — скриншоты не будут сделаны.
5. **Сложные выражения без контроля** — проверять, что выражения валидны JS.
6. **Неверный движок** — использовать `engineSupports` для атомов специфичных для движка.
7. **Игнорирование repeat/while** — помнить о переменных `$loop` и `repeat` в контексте.
8. **Пустые description** — всегда указывать осмысленные описания.

## 15) Порядок выполнения агента (внутренняя логика)

Для понимания поведения важно знать порядок выполнения в [src/Test.ts](src/Test.ts):

1. **Проверка needEnvParams** — валидация переменных окружения
2. **Сбор данных** — fetchData() собирает data/selectors из всех источников
3. **checkNeeds** — проверка needData/needSelectors
4. **Вычисление repeat/$loop** — если задан repeat
5. **Проверка if** — если false, агент пропускается
6. **Проверка errorIf** — если true, ошибка
7. **Сбор скриншотов** — если logOptions.screenshot
8. **Выполнение lifecycle** — beforeRun → beforeTest → run → runTest → afterRun → afterTest
9. **Обработка результатов** — allowResults, bindResults
10. **Проверка errorIfResult** — если true, ошибка
11. **Обработка while** — увеличение repeat при true
12. **Обработка repeat** — рекурсия при repeat > 1
13. **Проверка breakParentIfResult** — выброс ContinueParentError
14. **Обработка continueOnError** — логирование ошибки и продолжение

## 16) Screencast (запись видео)

PuppeDo поддерживает запись видео выполнения через screencast. Конфигурация в [tests/tests/screencast.yaml](tests/tests/screencast.yaml):

```yaml
name: screencastTest
runTest:
  - runnerSwitch:
      data:
        runnerName: mainRunner
      options:
        screencast: true  # Включить запись
  - goTo:
      bD: { url: baseUrl }
  - someAction:
      stepId: "action-1"  # stepId используется для связи с видео
```

`stepId` позволяет связать конкретный шаг с моментом в видео.

## 17) Расширение файлов (ext)

Механизм расширения позволяет разделять конфигурацию:

### 17.1 dataExt / selectorsExt
Подключение внешних data/selectors файлов к агенту:

```yaml
name: myTest
dataExt:
  - auth      # Подключить data с name: auth
  - config    # Подключить data с name: config
selectorsExt:
  - selectors # Подключить selectors с name: selectors
```

### 17.2 runnersExt
Расширение runner-а другим runner-ом:

```yaml
name: extendedRunner
type: runner
runnersExt:
  - mainRunner  # Наследовать настройки от mainRunner
browser:
  headless: true  # Переопределить headless
```

### 17.3 Приватные расширения
Для секретных данных (пароли, токены) используйте папку `private/`:

```yaml
# tests/private/auth.yaml
name: auth
type: data
data:
  username: real_user
  password: real_password
```

Папка `private/` обычно в `.gitignore`.

## 18) Атомы: лучшие практики создания

### 18.1 Структура атома
```yaml
name: myCustomAtom
type: atom
description: Описание что делает атом

# Документация
descriptionExtend:
  - "Подробное описание шага 1"
  - "Подробное описание шага 2"

# Валидация входных данных
needData:
  - requiredParam
  - optionalParam?  # Опциональный
needSelectors:
  - targetElement

# Разрешённые результаты
allowResults:
  - result1
  - result2

# Ограничения
allowOptions:
  - timeout
  - visible
engineSupports:
  - playwright

run:
  - actualAction:
      # Реализация
```

### 18.2 Композиция атомов
Создавайте сложные атомы из простых:

```yaml
name: loginUser
type: atom
description: Полная авторизация пользователя

needData:
  - username
  - password
needSelectors:
  - loginInput
  - passwordInput
  - submitButton

run:
  - typeInput:
      s: { selector: loginInput }
      d: { text: username }
  - typeInput:
      s: { selector: passwordInput }
      d: { text: password }
  - clickSelector:
      s: { selector: submitButton }
  - waitForSelector:
      s: { selector: dashboardElement }
```

## 19) Контрольные вопросы для ИИ перед генерацией workflow

**Анализ задачи:**
- Какие runners нужны и в каком порядке переключать?
- Какие данные/селекторы должны быть вынесены в ext‑файлы?
- Какие шаги должны быть в `before*` и `after*`?
- Где нужны результаты и какие ключи ожидаются?
- Какие проверки (`errorIf*`) нужны для валидации результата?
- Нужны ли скриншоты или логирование?
- Какие алиасы использовать для краткости?

**Технические аспекты:**
- Какой движок (playwright/puppeteer) будет использоваться?
- Есть ли iframe, требующие плагин `frame`?
- Нужны ли циклы (`repeat`/`while`)?
- Есть ли условное выполнение (`if`/`breakParentIfResult`)?
- Нужна ли обработка ошибок (`continueOnError`)?

**Качество:**
- Уникальны ли все имена агентов?
- Есть ли описания для каждого шага?
- Корректны ли JS-выражения?
- Указаны ли все обязательные параметры (`needData`/`needSelectors`)?

## 20) Полный пример: E2E тест авторизации

```yaml
# tests/tests/loginTest.yaml
---
name: loginTest
type: test
description: E2E тест авторизации пользователя

tags:
  - smoke
  - auth

dataExt:
  - auth  # Приватные данные (username, password)

selectorsExt:
  - selectors

beforeTest:
  - runnerSwitch:
      data:
        runnerName: mainRunner
  - goTo:
      bD:
        url: "baseUrl + '/login'"

runTest:
  # Ввод логина
  - typeInput:
      description: Ввод имени пользователя
      s:
        selector: loginPage.usernameInput
      d:
        text: username
      logOptions:
        screenshot: true

  # Ввод пароля
  - typeInput:
      description: Ввод пароля
      s:
        selector: loginPage.passwordInput
      d:
        text: password

  # Клик по кнопке входа
  - clickSelector:
      description: Нажатие кнопки входа
      s:
        selector: loginPage.submitButton

  # Проверка успешной авторизации
  - waitForSelector:
      description: Ожидание появления дашборда
      s:
        selector: dashboard.container
      options:
        timeout: 10000

  # Получение текста приветствия
  - getText:
      description: Получение текста приветствия
      s:
        selector: dashboard.welcomeMessage
      r:
        welcomeText: text
      errorIfResult: "!welcomeText.includes(username)"

afterTest:
  - runnerClose:
      data:
        runnerName: mainRunner
```

```yaml
# tests/data/selectors.yaml (фрагмент)
---
name: selectors
type: selectors
data:
  loginPage:
    usernameInput: "#username"
    passwordInput: "#password"
    submitButton: "button[type='submit']"
  dashboard:
    container: ".dashboard"
    welcomeMessage: ".welcome-message"
```

---

## Приложение A: Полный список алиасов

| Группа | Алиасы |
|--------|--------|
| **data** | `d`, `📋` |
| **bindData** | `bD`, `bd`, `📌📋`, `dataBind`, `db`, `dB`, `dataFunction`, `dF`, `df`, `🔑📋`, `functionData`, `fd`, `fD` |
| **selectors** | `selector`, `s`, `💠` |
| **bindSelectors** | `bindSelector`, `bS`, `bs`, `📌💠`, `selectorBind`, `selectorsBind`, `sb`, `sB`, `selectorsFunction`, `selectorFunction`, `sF`, `sf`, `🔑💠`, `functionSelector`, `functionSelectors`, `fs`, `fS` |
| **bindResults** | `bindResult`, `bR`, `br`, `result`, `results`, `r`, `↩️`, `R`, `rb`, `rB`, `resultBind`, `resultsBind`, `rF`, `rf`, `🔑↩️`, `functionResult`, `fr`, `fR`, `resultFunction`, `values`, `value`, `v`, `var`, `vars`, `const`, `c`, `let`, `set` |
| **options** | `option`, `opt`, `o`, `⚙️` |

---

## Приложение B: Встроенные атомы

Часто используемые атомы из `src/Plugins/`:

| Атом | Описание |
|------|----------|
| `blank` | Пустой контейнер для lifecycle/inlineJS |
| `goTo` | Переход по URL |
| `runnerSwitch` | Переключение runner |
| `runnerClose` | Закрытие runner |
| `clickSelector` | Клик по селектору |
| `typeInput` | Ввод текста |
| `getText` | Получение текста элемента |
| `waitForSelector` | Ожидание появления элемента |
| `waitForSelectorHidden` | Ожидание скрытия элемента |
| `getValue` | Получение значения input |
| `setValue` | Установка значения input |
| `scrollTo` | Прокрутка к элементу |
| `hover` | Наведение на элемент |
| `focus` | Фокус на элементе |
| `pressKey` | Нажатие клавиши |
| `selectOption` | Выбор опции в select |
| `checkCheckbox` | Установка состояния checkbox |
| `getAttribute` | Получение атрибута элемента |
| `evaluateExpression` | Выполнение JS в контексте страницы |

---

## Приложение C: Переменные окружения

| Переменная | Описание |
|------------|----------|
| `PPD_ROOT` | Корневая папка тестов |
| `PPD_ROOT_ADDITIONAL` | Дополнительные папки |
| `PPD_ROOT_IGNORE` | Игнорируемые папки |
| `PPD_FILES_IGNORE` | Игнорируемые файлы |
| `PPD_DATA` | Глобальные данные |
| `PPD_SELECTORS` | Глобальные селекторы |
| `PPD_TAGS_TO_RUN` | Теги для фильтрации |
| `PPD_CONTINUE_ON_ERROR_ENABLED` | Включить continueOnError |
| `PPD_LOG_DOCUMENTATION_MODE` | Режим генерации документации |
| `PPD_DEBUG_MODE` | Режим отладки |
| `PPD_LOG_LEVEL_NESTED` | Уровень вложенности логов |
| `PPD_LOG_SCREENSHOT` | Скриншоты по умолчанию |
| `PPD_LOG_FULLPAGE` | Полностраничные скриншоты |

---

Документ опирается на текущую конфигурацию и код проекта. При изменениях `PPD_ALIASES` или `PPD_LIFE_CYCLE_FUNCTIONS` необходимо актуализировать соответствующие разделы.
