name: clickWithNestedWait
type: atom
needSelectors: ['mainInputInside']
description: Nested Click Atom
logOptions:
  textColor: magenta
  backgroundColor: yellow

runTest:
  - waitForSelector:
      bS: { selector: mainInputInside }

  - clickSelector:
      bS: { selector: mainInputInside }
