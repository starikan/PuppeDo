name: clickWithNestedWait
type: atom
needSelectors: ['wikiInputInside']
runTest:
  - waitForSelector:
      bS: { selector: wikiInputInside }

  - clickSelector:
      bS: { selector: wikiInputInside }
