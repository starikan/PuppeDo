name: continueParentIfResult
runTest:
  - blank:
      description: First level

  - blank:
      description: First level to

  - loop:
      repeat: 3
      runTest:
        - blank:
            bindDescription: "`Second level loop: ${$loop}`"
            continueParentIfResult: "$loop < 3"

        - blank:
            description: "I`m next"

  - blank:
      description: Another First level