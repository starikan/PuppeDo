name: runnerRunClose
description: Some runners manipulations
runTest:
  - case:
      description: Open 2 env and close them
      runTest:
        - runnerSwitch:
            data: { runnerName: mainEnv }

        - runnerClose:
            data: { runnerName: mainEnv }
